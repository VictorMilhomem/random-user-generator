import React from 'react';
import './GenButton.css';


const GenButton = ({onClick}) => {

    return (

        <button onClick={onClick}>Generate User</button>

    )

}


export default GenButton;
